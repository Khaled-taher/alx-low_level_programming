#include "main.h"

/**
 * main - start of tthe code
 *
 * Return: always return (0)
 */
int main(void)
{
	_printf("");
	_printf("this is %s first try, this is a first letter of my name %c, my garde is %d.%i%%, in binary %b and %b\n", "my", 'K', 148, 56, 16, 7);
	_printf("%");
	return (0);
}
