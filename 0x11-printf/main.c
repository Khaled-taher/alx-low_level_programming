#include "main.h"

/**
 * main - start of tthe code
 *
 * Return: always return (0)
 */
int main(void)
{
	unsigned int x = -1;

	_printf("");
	_printf("this is %s first try, this is a first letter of my name %c, my garde is %d in minus form %i%%, in binary %b and %b\n", "my", 'K', 148, -148, 16, 7);
	_printf("%i %u %b %x %X %x %X\n", x, x, x, x, x, 15, 16);
	_printf("%");
	return (0);
}
